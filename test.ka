comp City:
    #adyubd, #z, #x: scalar;
    #adyubd, #z, #x: scalar;
    #adyubd, #z, #x: boolean;
    #adyubd, #z, #x: str;
    #y: scalar;
    #a : integer;

    def bla(n: integer):
        #a = 3;
        #a  += #a * n;
        #y = 3**2;
    enddef;

    def koko():
        yy : integer;
        yy = 3;
        #a  += #a * n;
        #y = #y**yy;
    enddef;
endcomp;

comp Adress:
    #adyubd: boolean;
    #bla[3], #x[4], #x[4]: str;
    #a[], #b[], #x[] : str;

    def lolo(i: str):
       #adyubd = True;
    enddef; 
endcomp;

const x = 3: integer;

orderId: integer;
def foo():
;
enddef;
def main():

enddef;


